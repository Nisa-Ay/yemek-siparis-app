import 'package:flutter/material.dart';

class Hesabim extends StatelessWidget {
  const Hesabim({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Hesabım'));
  }
}